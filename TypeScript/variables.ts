// To store variable as typescript name suggest that, this is strict language in the form of type of 
// all the implementation which we do
// So to declare any variable define its type as well

let n1: number = 10;
let str: string = "hello";

console.log(n1);
console.log(str);

export {};
